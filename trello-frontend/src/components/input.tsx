

const Input = (props) => {
  return (
     <input type={props.type} placeholder ={props.placeholder} style={{ minWidth: "300px", padding: "10px", margin: "10px", borderRadius: "4px" }}/>
    
  )
}

export default Input